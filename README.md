# sanjanamaini.github.io

This is Sanjana Maini's actual **GitHub Pages** repository — the special `<username>.github.io` repo name means GitHub will auto-publish whatever static site lives here at `https://sanjanamaini.github.io`. A `CNAME` file is already committed, pointing the custom domain **sanjanamaini.me** at this Pages site.

## Current state

The repo has exactly one file besides this README: `CNAME` (containing `sanjanamaini.me`). **There is no site content yet** — no `index.html`, no CSS/JS, no build config. The domain is wired up, but nothing is deployed to it.

## What's Here

- `CNAME` — custom domain configuration for GitHub Pages (`sanjanamaini.me`).

## How this differs from `myportfolio`

`myportfolio` is a separate, ordinary (non-Pages) repo that currently contains someone else's portfolio site (see that repo's README for detail) — it is not connected to any live URL by virtue of its name. This repo, `sanjanamaini.github.io`, is the one GitHub will actually serve at sanjanamaini.me once real site files are added here.

## Setup/Run

Not applicable yet — there's no site to build or run. To make this live, add a static site (plain HTML/CSS/JS, or a generated `docs/`/root output from a framework) at the repo root and push to the default branch; GitHub Pages will serve it automatically at the CNAME domain.
